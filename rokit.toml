# Toolchain pins for local development (install rokit from https://github.com/rojo-rbx/rokit,
# then run `rokit install` in this folder). Not required just to open build/game.rbxlx in Studio.

[tools]
rojo = "rojo-rbx/rojo@7.7.0"
stylua = "JohnnyMorganz/StyLua@2.1.0"
selene = "Kampfkarren/selene@0.28.0"
