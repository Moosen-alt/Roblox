# Rojo build artifacts are committed on purpose (build/game.rbxlx in each game)
# so the games can be opened in Studio without any local tooling. Everything
# else transient:
*.rbxlx.lock
*.rbxl.lock
sourcemap.json
/DataStore
.DS_Store
